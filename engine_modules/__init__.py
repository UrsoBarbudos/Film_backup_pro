"""
Пакет модулей движка (engine).

Важно: в проекте уже существует корневой файл `engine.py`, который остаётся фасадом
для обратной совместимости. Поэтому реальная логика вынесена сюда, в `engine_modules/`.
"""

